# Security Headers
add_header X-Frame-Options "SAMEORIGIN" always;
add_header X-Content-Type-Options "nosniff" always;
add_header Referrer-Policy "strict-origin-when-cross-origin" always;
add_header Permissions-Policy "geolocation=(), microphone=(), camera=()" always;
add_header Strict-Transport-Security "max-age=31536000; includeSubDomains; preload" always;

# CORS Headers (only for API routes, not all routes)
# Note: These should be handled by your backend, not Nginx
# Commenting out to avoid conflicts with Next.js/Better Auth

# add_header Access-Control-Allow-Origin * always;
# add_header Access-Control-Allow-Credentials true always;
# add_header Access-Control-Allow-Methods "GET, POST, OPTIONS, PUT, DELETE" always;
# add_header Access-Control-Allow-Headers "Authorization, Content-Type, Set-Cookie, X-Requested-With, X-Auth-Token" always;

# Note: Don't override Set-Cookie headers - let the application handle them
# Note: Cache-Control is handled in the main config with the $cache_control variable
